apiVersion: v1
kind: Pod
metadata:
  name: nginx-max-test
  namespace: datarc-dev

spec:
  containers:
  - name: nginx
    image: shixiaochuangk8s/nginx:1.18

    resources:
      limits:
        cpu: "2"
        memory: 2Gi