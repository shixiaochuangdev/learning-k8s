apiVersion: v1
kind: Service
metadata:
  name: service-clusterip
  namespace: service-ns
spec:
  selector:
    app: nginx-pod-yaml
  type: ClusterIP
  ports:
  - port: 80  # Service端口
    targetPort: 80 # pod端口