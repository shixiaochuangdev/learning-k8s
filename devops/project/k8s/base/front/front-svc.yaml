apiVersion: v1
kind: Service
metadata:
  name: exam-system-web
  namespace: exam-system
spec:
  type: ClusterIP
  selector:
    app: exam-system-web
  ports:
    - name: http
      port: 80
      targetPort: 80