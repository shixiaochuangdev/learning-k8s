apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: exam-system
  namespace: exam-system
spec:
  gatewayClassName: traefik
  listeners:
    - name: web
      protocol: HTTP
      port: 80