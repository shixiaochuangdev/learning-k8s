apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroutewrr
  namespace: traefik-ns

spec:
  entryPoints:
    - web
  routes:
  - match: Host(`can.shixiaochuang.org`) && PathPrefix(`/`)
    kind: Rule
    services:
    - name: wrr
      kind: TraefikService