apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: traefikui-redirect-dashboard
  namespace: traefik
spec:
  redirectRegex:
    regex: ^/$
    replacement: /dashboard/
    permanent: true