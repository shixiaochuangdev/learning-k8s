apiVersion: apps/v1
kind: Deployment
metadata:
  name: sl-demo-app
  namespace: kustomize-ns
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            - name: "DB_PASSWORD"
              valueFrom:
                secretKeyRef:
                  name: sl-demo-app
                  key: db.password
